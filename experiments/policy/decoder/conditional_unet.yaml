# Conditional U-Net Decoder

_target_: refactoring.models.decoding.decoders.factory.conditional_unet_decoder.ConditionalUNetDecoder

# Input features from encoding pipeline
input_keys:
  - fused_features     

# Diffusion timestep embedding dimension
embedding_dimension: 256

# U-Net downsampling channel dimensions
down_dimensions:
  - 256
  - 512
  - 1024

# Convolution kernel size in residual blocks
kernel_size: 5

# Number of groups for group normalization
num_groups: 8

# Local conditioning (sequence-aligned features)
# Currently not implemented - use global conditioning only
use_local_conditioning: false

# Whether conditions predict scaling factors in FiLM
condition_predict_scale: true

action_heads:
  position_action:
    _target_: refactoring.models.decoding.action_heads.ActionHead
    input_dim: ${task.action_space.position_dim}
    output_dim: ${task.action_space.position_dim}
    blocks: null
  gripper_action:
    _target_: refactoring.models.decoding.action_heads.ActionHead
    input_dim: ${task.action_space.gripper_dim}
    output_dim: ${task.action_space.gripper_dim}
    blocks: null

observation_horizon: ${task.observation_horizon}
prediction_horizon: ${task.prediction_horizon}
action_space: ${task.action_space}
observation_space: ${task.observation_space}
device: ${experiment.device}