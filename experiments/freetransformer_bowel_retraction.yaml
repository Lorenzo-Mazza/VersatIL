# Free Transformer experiment for bowel retraction task
# Uses discrete latent variables for multi-modal action prediction

defaults:
  - experiment: bowel_retraction_act
  - task/dataset_schema: bowel_retraction_v2
  - task/dataloader: act_default
  - task/action_space: br_position_gripper_deltas_cf
  - task/observation_space: br_rgb_proprio_cf
  - training: act_default
  - policy/encoding_pipeline: rgb_resnet18_proprio_mlp_fusion
  - policy/decoder: free_transformer
  - policy/algorithm: behavioral_cloning
  - policy/loss: free_transformer_default
  - inference: act_default
  - _self_

task:
  observation_horizon: 4 # Number of past observations
  prediction_horizon: 10  # Chunk size

policy:
  _target_: refactoring.models.policy.Policy
  observation_space: ${task.observation_space}
  action_space: ${task.action_space}
  prediction_horizon: ${task.prediction_horizon}
  device: ${experiment.device}
  validate_loss_keys: true

# Override specific parameters from defaults/command line if needed
# For example, to use different batch size:
# task.dataloader.batch_size: 64
