defaults:
  - csv
  - zarr_meta/bowel_retraction@metadata
  - _self_

_target_: versatil.data.raw.schemas.custom.bowel_retraction.BowelRetractionSchema
zarr_path: ${zarr_dir:bowel_retraction/phantom_exvivo/dataset.zarr}
dataset_folders:
  - ${bowel_retraction_dir:v1}
  - ${bowel_retraction_dir:v2}
  - ${bowel_retraction_dir:ex_vivo}
  - ${bowel_retraction_dir:ex_vivo2}
  - ${bowel_retraction_dir:multi_camera_exp}
  - ${bowel_retraction_dir:multi_camera_exp2}