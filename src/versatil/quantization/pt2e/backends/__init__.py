"""PT2E backend implementations for quantized operator lowering.

Consumers should import directly from submodules:
  - ``versatil.quantization.pt2e.backends.base`` for ``BasePT2EBackend``
  - ``versatil.quantization.pt2e.backends.x86_inductor`` (requires torchao)
"""
