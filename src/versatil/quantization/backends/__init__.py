"""PT2E backend implementations for quantized operator lowering.

Consumers should import directly from submodules:
  - ``versatil.quantization.backends.base`` for ``BasePT2EBackend``
  - ``versatil.quantization.backends.x86_inductor`` (requires torchao)
"""
