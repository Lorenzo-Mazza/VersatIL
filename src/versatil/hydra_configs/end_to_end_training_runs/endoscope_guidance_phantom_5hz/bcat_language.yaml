# @package _global_
_target_: versatil.configs.main.MainConfig

defaults:
  - /task: base
  - /policy: base
  - /experiment: default
  - /task/dataset_schema: endoscope_guidance_phantom_5hz_lerobot
  - /task/dataloader: endoscope_guidance
  - /task/action_space: camera_frame_position_and_roll_precomputed_lerobot
  - /task/observation_space: stereo_rgb_language_tso_lerobot
  - /training: default
  - /policy/encoding_pipeline: stereo_rgb_language_encoder
  - /policy/decoder: action_transformer_language
  - /policy/algorithm: behavioral_cloning
  - /policy/loss: regression_camera_frame_position_and_roll_precomputed_lerobot
  - override /policy/decoder/heads@policy.decoder.action_heads: camera_frame_position_and_roll_precomputed_lerobot
  - _self_

task:
  observation_horizon: 1
  prediction_horizon: 10
  dataloader:
    tokenization:
      observation_tokenizer:
        tokenizer_model: ${language_model:MINI_LM_L6}

experiment:
  precision: ${precision:FP32}
  checkpoint_folder: ${checkpoint_dir:endoscope_guidance_phantom_5hz}
  checkpoint_every: 5

training:
  num_epochs: 100
  early_stopping_patience: 200
  lr_schedule: null
  clip_gradient_norm: true
  clip_max_norm: 1.0

policy:
  _target_: versatil.models.policy.Policy
  observation_space: ${task.observation_space}
  action_space: ${task.action_space}
  prediction_horizon: ${task.prediction_horizon}
  observation_horizon: ${task.observation_horizon}
  device: ${experiment.device}
  decoder:
    embedding_dimension: 256
    feedforward_dimension: 1024
    number_of_layers: 4
  encoding_pipeline:
    encoders:
      instruction:
        model_name: ${language_model:MINI_LM_L6}
