# @package _global_
_target_: versatil.configs.main.MainConfig

defaults:
  - /task: base
  - /policy: base
  - /experiment: default
  - /task/dataset_schema: endoscope_guidance_phantom_5hz
  - /task/dataloader: libero_lerobot
  - /task/action_space: endoscope_position_roll_precomputed
  - /task/observation_space: endoscope_stereo_rgb_language
  - /training: default
  - /policy/encoding_pipeline: stereo_rgb_language_encoder_tiny
  - /policy/decoder: action_transformer_language
  - /policy/algorithm: behavioral_cloning
  - /policy/loss: regression_endoscope_position_roll
  - override /policy/decoder/heads@policy.decoder.action_heads: endoscope_position_roll
  - _self_

task:
  observation_horizon: 1
  prediction_horizon: 10
  dataloader:
    tokenization:
      observation_tokenizer:
        tokenizer_model: ${language_model:MINI_LM_L6}

experiment:
  precision: ${precision:FP32}
  checkpoint_folder: ${checkpoint_dir:endoscope_guidance_phantom_5hz}
  checkpoint_every: 5

training:
  num_epochs: 100
  early_stopping_patience: 200
  lr_schedule: null
  clip_gradient_norm: true
  clip_max_norm: 1.0

policy:
  _target_: versatil.models.policy.Policy
  observation_space: ${task.observation_space}
  action_space: ${task.action_space}
  prediction_horizon: ${task.prediction_horizon}
  observation_horizon: ${task.observation_horizon}
  device: ${experiment.device}
  loss:
    loss_modules:
      regression_loss:
        action_keys:
          - ${proprio_key:CAMERA_FRAME_CARTESIAN_TIP_POS}
          - ${proprio_key:ENDOSCOPE_ROLL_ACTION}
      trajectory_length:
        action_key: ${proprio_key:CAMERA_FRAME_CARTESIAN_TIP_POS}
  encoding_pipeline:
    encoders:
      instruction:
        model_name: ${language_model:MINI_LM_L6}
