defaults:
  - /policy/decoder/action_head@joint_action: base
  - _self_

joint_action:
  input_dimension: ${int_mul:${policy.decoder.expert_width_multiplier},${vlm_hidden_dimension:${policy.decoder.vlm_backbone.model_name}}}
  blocks: null
