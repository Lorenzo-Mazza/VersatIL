defaults:
  - /policy/decoder/action_head@joint_action: base
  - _self_

joint_action:
  input_dimension: ${mul:${action_space_prediction_dimension:${task.action_space}},4096}
  blocks:
    - _target_: versatil.models.decoding.action_heads.blocks.MLPBlock
      input_dimension: ${...input_dimension}
      hidden_dimensions:
        - 4096
      output_dim: null
      activation: ${activation_function:RELU}
      dropout: 0.0
      normalization: true
    - _target_: versatil.models.decoding.action_heads.blocks.ResidualBlock
      block:
        _target_: versatil.models.decoding.action_heads.blocks.MLPBlock
        input_dimension: 4096
        hidden_dimensions:
          - 4096
        output_dim: null
        activation: ${activation_function:RELU}
        dropout: 0.0
        normalization: true
      dropout: 0.0
    - _target_: versatil.models.decoding.action_heads.blocks.ResidualBlock
      block:
        _target_: versatil.models.decoding.action_heads.blocks.MLPBlock
        input_dimension: 4096
        hidden_dimensions:
          - 4096
        output_dim: null
        activation: ${activation_function:RELU}
        dropout: 0.0
        normalization: true
      dropout: 0.0
    - _target_: versatil.models.decoding.action_heads.blocks.LayerNormBlock
      input_dimension: 4096
