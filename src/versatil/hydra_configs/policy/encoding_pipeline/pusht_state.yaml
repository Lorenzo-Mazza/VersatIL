defaults:
  - encoder/proprio@encoders.robot_state
  - _self_

encoders:
  robot_state:
    input_keys:
      - ${proprio_key:EE_POS}
      - ${proprio_key:PUSHT_KEYPOINTS}
    output_dim: 64

fusion_stages: []
