defaults:
  - synthetic_schema
  - /task/dataset_schema/zarr_meta/synthetic@metadata

_target_: versatil.data.raw.schemas.custom.synthetic.SyntheticSchema
dataset_type: ${dataset_type:SYNTHETIC}
zarr_path: ${zarr_dir:synthetic/radial_k${task.dataset_schema.num_modes}.zarr}
task_name: ${synthetic_task:RADIAL}
num_episodes: 1000
seed: 42
image_size: 64
num_modes: 8
trajectory_length: 60
noise_std: 0.006
num_styles: 1
num_rollouts: 200
