defaults:
  - synthetic_schema
  - /task/dataset_schema/zarr_meta/synthetic@metadata

_target_: versatil.data.raw.schemas.custom.synthetic.SyntheticSchema
dataset_type: ${dataset_type:SYNTHETIC}
zarr_path: ${zarr_dir:synthetic/sequential_decision.zarr}
task_name: ${synthetic_task:SEQUENTIAL_DECISION}
num_episodes: 1000
seed: 42
image_size: 64
num_modes: 4
trajectory_length: 60
noise_std: 0.012
num_styles: 1
num_rollouts: 200
