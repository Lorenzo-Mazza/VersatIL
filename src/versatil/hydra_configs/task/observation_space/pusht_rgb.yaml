defaults:
  - base

_target_: versatil.data.task.ObservationSpace

observations_metadata:
  ${cameras:AGENTVIEW}:
    _target_: versatil.data.metadata.RGBCameraMetadata
    camera_key: ${raw_camera:IMAGE_PUSHT}
    dtype: uint8
    image_width: 96
    image_height: 96
