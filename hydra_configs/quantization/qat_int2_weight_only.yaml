defaults:
  - /quantization/workflow@_here_: eager
  - _self_

quantize_config:
  _target_: torchao.quantization.IntxWeightOnlyConfig
  weight_dtype: ${torch_dtype:int2}
  granularity:
    _target_: torchao.quantization.PerGroup
    group_size: 32
is_qat: true
module_paths: []
auto_filter_incompatible_linears: true
