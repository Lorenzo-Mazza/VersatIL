defaults:
  - synthetic_schema
  - zarr_meta/synthetic@metadata

_target_: versatil.data.raw.schemas.custom.synthetic.SyntheticSchema
dataset_type: ${dataset_type:SYNTHETIC}
zarr_path: ${zarr_dir:synthetic/trajectory_style.zarr}
task_name: ${synthetic_task:TRAJECTORY_STYLE}
num_episodes: 1000
seed: 42
image_size: 64
num_modes: 3
trajectory_length: 60
noise_std: 0.015
num_styles: 4