defaults:
  - base

_target_: versatil.data.task.ObservationSpace

observations_metadata:
  ${cameras:SYNTHETIC_TOP}:
    _target_: versatil.data.metadata.CameraMetadata
    camera_key: ${cameras:SYNTHETIC_TOP}
    dtype: uint8
    channels: 3
    image_width: 64
    image_height: 64