# @package _global_

defaults:
  - action_transformer_language
  - /quantization: qat_int8_dynamic_intx_int4
  - _self_

policy:
  encoding_pipeline:
    encoders:
      instruction:
        use_embeddings_only: false
