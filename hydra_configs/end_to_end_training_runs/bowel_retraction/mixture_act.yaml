# @package _global_
_target_: versatil.configs.main.MainConfig

defaults:
  - /task: base
  - /policy: base
  - /experiment: default
  - /task/dataset_schema: bowel_retraction_v2
  - /task/dataloader: bowel_retraction_language
  - /task/action_space: deltas_cf_pos_gripper_phase
  - /task/observation_space: stereo_rgb_language_proprio_cf
  - /training: default
  - /policy/encoding_pipeline: stereo_rgb_language_encoder
  - /policy/decoder: mixture_of_act
  - /policy/algorithm: bc_with_learned_prior
  - /policy/loss: moe_regression_gripper_MMD
  - /inference: default
  - _self_

experiment:
  checkpoint_folder: ${checkpoint_dir:bowel_retraction}

task:
  observation_horizon: 1
  prediction_horizon: 10
  action_space:
    actions_metadata:
      ${tso_obs_key:PHASE_LABEL}:
        requires_prediction_head: false

policy:
  _target_: versatil.models.policy.Policy
  observation_space: ${task.observation_space}
  action_space: ${task.action_space}
  prediction_horizon: ${task.prediction_horizon}
  observation_horizon: ${task.observation_horizon}
  device: ${experiment.device}
  validate_loss_keys: true
  loss:
    loss_modules:
      phase_passthrough:
        _target_: versatil.metrics.components.MetadataPassthrough
        keys_mapping:
          ${tso_obs_key:PHASE_LABEL}: ${metadata_key:PHASE_LABEL}
  decoder:
    base_expert:
      input_keys:
        - left_rgb
        - right_rgb
        - instruction_language

# Override specific parameters from defaults/command line if needed
# For example, to use different batch size:
# task.dataloader.batch_size: 64
# To override specific config components from command line, you can do for example
# ```policy/loss=mmd_l1_pos_gripper_bce``` to use MMD instead of KL in your loss
