# @package _global_
# Flow Matching with DiT Cross-Attention Transformer - 112x112 RGB only (Q-FAT Image Kitchen style)
# 9-DoF Franka (relative velocity actions), rendered MuJoCo frames at 10 Hz
_target_: versatil.configs.main.MainConfig

defaults:
  - /task: base
  - /policy: base
  - /experiment: default
  - /task/dataset_schema: kitchen
  - /task/dataloader: kitchen_rgb
  - /task/action_space: kitchen_precomputed
  - /task/observation_space: kitchen_rgb
  - /training: ema
  - /policy/encoding_pipeline: kitchen_rgb
  - /policy/decoder: dit_cross_attention_transformer
  - /policy/algorithm: flow_matching
  - /policy/loss: regression_synthetic
  - /inference: default
  - override /policy/decoder/heads@policy.decoder.action_heads: kitchen_9d
  - _self_

task:
  observation_horizon: 1
  prediction_horizon: 10
  dataloader:
    val_ratio: 0.0
    preload_data_in_memory: true

experiment:
  checkpoint_folder: ${checkpoint_dir:kitchen}

training:
  num_epochs: 300
  early_stopping_patience: 500

policy:
  _target_: versatil.models.policy.Policy
  observation_space: ${task.observation_space}
  action_space: ${task.action_space}
  prediction_horizon: ${task.prediction_horizon}
  observation_horizon: ${task.observation_horizon}
  device: ${experiment.device}
  validate_loss_keys: true
  loss:
    loss_modules:
      regression_loss:
        action_keys:
          - ${proprio_key:KITCHEN_ARM_ACTION}
        mse_weight: 1.0
        l1_weight: 0.0
        huber_weight: 0.0
      trajectory_length:
        weight: 0.0
        action_key: ${proprio_key:KITCHEN_ARM_ACTION}
  encoding_pipeline:
    encoders:
      left:
        input_keys:
          - ${cameras:AGENTVIEW}
  decoder:
    input_keys:
      - left_rgb
    embedding_dimension: 256
    timestep_embedding_dimension: 128
    number_of_heads: 8
    number_of_layers: 8
    feedforward_dimension: 1024
    dropout_rate: 0.1
    attention_dropout: 0.01
