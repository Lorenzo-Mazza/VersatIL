# @package _global_
# Plain task bundle (no latent_dimension override). For non-variational
# algorithms (flow matching, MDN) that have no posterior_encoder/prior.
defaults:
  - override /task/dataset_schema: synthetic/circle
  - override /task/observation_space: synthetic_image_state
  - override /policy/encoding_pipeline: mono_rgb
