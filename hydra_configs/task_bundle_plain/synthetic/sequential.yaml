# @package _global_
defaults:
  - override /task/dataset_schema: synthetic/sequential
  - override /task/observation_space: synthetic_image_state
  - override /policy/encoding_pipeline: mono_rgb

training:
  num_epochs: 4000
